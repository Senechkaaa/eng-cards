export enum Routes {
    HOME = '/',
    AUTH = '/auth',
    CARDS = '/cards',
    PROFILE = '/profile',
    LIBRARY = '/library',
    CREATE = `${CARDS}/create`,
    LEARN = `${CARDS}/learn`,
    SUPPORT = '/support',
    PREMIUM = `${PROFILE}/premium`,
    FAQ = '/faq',
}
